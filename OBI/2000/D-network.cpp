#include <iostream>

using namespace std;

int main(int argc, char *argv[]) {
    long long n, m;
    
    while (cin >> n >> m, n != 0) {
        for (int i = 0; i < m; i++) {
            cin >> a >> b >> c;
            
        }
    }
    return 0;
}