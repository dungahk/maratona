#include <bits/stdc++.h>

using namespace std;

int main(int argc, char *argv[]) {
    long long l, r;
    
    while (cin >> l >> r, l != 0) {
        cout << l + r << endl;
    }
    
    return 0;
}
