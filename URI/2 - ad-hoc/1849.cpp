#include <bits/stdc++.h>

#define _ ios_base::sync_with_stdio(0);cin.tie(0);

typedef long long ll;
typedef unsigned long long ull;

using namespace std;

int main(int argc, char *argv[]) { _
    ll ld, cd, lv, cv, i, area, totalArea;
    
    cin >> ld >> cd >> lv >> cv;
    
    
    
    cout << area << endl;
    
    // cout << endl;
    return 0;
}
